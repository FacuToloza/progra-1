#include <stdio.h>
#include <stdlib.h>

int elevar(int x, int cant);

int main()
{

    int x, i, tol = 2, resultado;
    printf("Ingrese numero x: ");
    scanf("%d",&x);
    while()
    {
        for(i=0;i<tol;i++)
        {
            resultado = elevar(x,i)
        }
    }
    return 0;
}
}

int elevar(int x, int cant)
{
    int i,res;
    for(i=1;i<=cant;i++)
}

